# dive-gstack-k4

**Kind:** work

## Goal

Deep-dive **garrytan/gstack** (https://github.com/garrytan/gstack) as a survey
source. It is dual-high: a `SKILL.md` + `SKILL.md.tmpl` + `skillify` authoring
toolchain (skills), and a fully-worked staged sprint pipeline with verification
gates (grove). End with a **takeaway-for-skills** and a **takeaway-for-grove**.

## Context

- Shortlist rank #1 (`docs/research/skill-repo-prior-art.md` §1a). Named seed.
  Verified 114,963★ (GitHub API, 2026-06-25).
- Read the node `BRIEF.md` for the full downstream questions + search discipline,
  and `CONTEXT.md` for the skills|grove target split.

## Done when

- A `## garrytan/gstack` section is appended to
  `docs/research/skill-repo-prior-art.md` with cited findings (primary-source
  quote per claim), each tagged **target** (skills|grove) + a walk-away note,
  ending with takeaway-for-skills / takeaway-for-grove.

## Notes

- Focus — **skills Q2** (how `skillify` + `SKILL.md.tmpl` author/scaffold skills)
  and **grove Q5/Q6** (the `autoplan` → `plan-*-review` gates → `review`/`qa` →
  `ship` → `retro` pipeline; `canary`/`careful`/`guard` doubt/verification gates —
  do any improve grove's loop, decomposition, or retire/finish cycle?).
- It's a Claude-Code slash-command repo; quote the actual command files, not the
  README's framing.
