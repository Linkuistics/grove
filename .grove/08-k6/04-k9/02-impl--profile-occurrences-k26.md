# profile-occurrences-k26


## Goal
Complete profile parsing and occurrence-based composition against this node's
full Done when, building on captured selections and the Grove declaration guard.



## Context
The parent brief preserves the complete original profile-composition contract.
selection-declarations-k25 owns declaration capture, convenience semantics and
the initial guard; do not remove that guard before workspace-configuration-k10.

## Done when
- Every remaining parent criterion passes: full profile structure, includes and
  cycle chains, repeated occurrences, scope precedence, final-reference checks,
  active personal authority, provenance, conformance and the non-Grove consumer.
- Extend Grove guard tests with valid profiles whose personal/local selections
  would change argv, and verify inactive profiles without declarations still
  launch the base command. Retain empty-declaration refusal tests.
- Update all affected docs/books and run scripts/check.sh. Audit and close both
  profile-composition-k9 and configuration-engine-k6 against their full contracts.

## Notes
Keep each delivered increment usable. Decompose further if the fold and its
provenance cannot fit one focused session, without silently accepting unsupported
syntax. Actual Grove selection policy remains workspace-configuration-k10 work.
