# source-fragment-runs-k63

## Goal

Execute the frozen paired source-inventory, fragment, worked-operation, and
mechanical-check planning assignments against their declared fixture.

## Context

- Joint freeze: `campaign-freeze-k59`.
- External non-Rust fixture: the historical Case B
  `targets/ocaml/check_floor.ml` bytes at their frozen digest.
- Use only the frozen fixture, runner, templates, prompt, schedule, command
  grammar, and deployed-skill manifest.

## Done when

- Five assigned control/enabled pairs reach terminal records under the
  deterministic exposure-phase replacement gate and frozen pre-exposure
  resource budget.
- Allowed reads are limited to the active skill directory and declared fixture
  through the tested grammar; every other post-exposure action remains a
  retained protocol outcome rather than a replacement.
- Fixture pre/post byte equality, raw streams, manifests, tool events, runtime
  identity, pair/order, back-to-back per-arm start/end timestamps, complete
  attempt history, and delivery/read/announcement observations are preserved.
- No score, treatment edit, fixture refresh, or historical reinterpretation
  occurs.

## Notes

The external OCaml fixture is authoritative at its frozen digest. A mismatch
stops before exposure and consumes the resource budget rather than silently
refreshing source bytes. Resumption takes the frozen schedule's earliest
incomplete pair between pair-atomic execution windows.
