# exposition-runs-k65

## Goal

Execute the frozen paired exposition, local-completeness, prose, validation,
and independent-review planning assignments.

## Context

- Joint freeze: `campaign-freeze-k59`.
- Use only the frozen runner, templates, prompt, schedule, access grammar, and
  deployed-skill manifest.

## Done when

- Five assigned control/enabled pairs reach terminal records with only proven
  pre-exposure failures eligible for replacement under the deterministic
  exposure-phase gate and frozen resource budget.
- Legal target-skill discovery is preserved and reported; every post-exposure
  refusal, access violation, timeout, truncation, missing final, or non-discovery
  remains an outcome.
- Raw streams, manifests, prompt bytes, runtime identity, pair/order, attempt
  history, back-to-back per-arm start/end timestamps, and
  delivery/read/announcement observations are complete.
- No score, skill refinement, or early cross-case conclusion occurs.

## Notes

The prompt supplies only the frozen codebase facts. Invented implementation
details remain substantive behavior to score, not a reason to replace a run.
Automatic resumption occurs only between pair-atomic windows and takes the
earliest incomplete pair in frozen schedule order without inspecting arm or
outcome. Mid-pair reserve exhaustion makes that pair terminally unavailable.
