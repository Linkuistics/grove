# acceptance-contract-reconciliation-k79

**Integrates:** acceptance-contract-reconciliation-k78

## Goal

Apply the `acceptance-contract-reconciliation-k78` findings to the reconciled
acceptance contract, `acceptance-replication-authority-k76`, and the
`recovery-campaign-k54` subtree, so the requirements decision is taken with the
Case A instrument defect and the apparatus failure visible, and so no evaluated
context runs on an apparatus proven to invalidate itself.

## Context

- Findings, verbatim and severity-ordered: the `## Findings` section of
  `acceptance-contract-reconciliation-k78`, together with its
  `## Checks that passed`, which records what the integration need not re-derive.
- Producer artifact being corrected: `acceptance-contract-reconciliation-k75`
  and the `recovery-campaign-k54` subtree it reshaped.
- Parent acceptance contract: `walkthrough-skill-delivery-k19` and the root
  brief.
- Evidence the findings are grounded in, all immutable:
  `docs/evaluations/writing-code-walkthroughs/README.md`, the byte-frozen
  `docs/evaluations/writing-code-walkthroughs/baseline/rubric.md`, and the
  frozen records under `enabled/transfer-probe/`, `enabled/source-fragments/`,
  and `baseline/source-fragments/`.
- Prior findings and dispositions this cycle builds on: `evaluation-recovery-k73`
  and `evaluation-recovery-k74`.
- No evaluated treatment or control context may run in or before this leaf.

## Done when

- **F1** — `acceptance-replication-authority-k76`'s `Done when` requires the
  decision to confront `README.md:247`-`251` explicitly and to record which of
  the three named readings it takes: option 1 with the Case A selection
  limitation carried as a non-removable clause of any discharge, option 2
  recording behavioral acceptance as failed, or an explicit amendment to the
  Case A access rule. If option 1 is taken, `campaign-report-k72` carries that
  limitation beside the superseded-digest and shortfall limitations it already
  preserves. k75's recommendation text is updated so it no longer reads as
  settled against the finding.
- **F2** — one named leaf owns enumerating every model-interface surface
  reachable from the sealed control and enabled templates under the frozen
  command shape, proving that the surfaces the frozen boundary already forbids
  are absent before any attempt, and recording that enumeration in
  `campaign-freeze-k59`. The disposition states that this is apparatus
  conformance with `rubric.md:66`-`69` and `rubric.md:89`-`93`, not a new rule,
  and it names what an unobtainable `codex-cli 0.150.1` means for the
  replication.
- **F3** — settled explicitly one way and recorded: either the cross-codebase
  transfer conjunct is added to `walkthrough-skill-delivery-k19`'s `Done when`
  as an amendment routed through `k76`, or the probe is demoted to separately
  reported generality evidence in `k54`, `k68`, `k70`, and `k72` and its ten
  evaluated repetitions are re-tested against k73's F4 cost argument. The
  promotion of a `Context` sentence to an acceptance clause does not survive
  undocumented in either branch.
- **F4** — `measurement-design-k55`, the `same-case-evidence-k60` brief,
  `source-fragment-adjudication-k64`, `exposition-adjudication-k66`,
  `same-case-verdict-k67`, and `campaign-report-k72` state that Cases B and C
  can yield only descriptive per-row counts and terminal shortfall records under
  the frozen rubric, that material and regression classification is undefined
  for them, and that "undefined" is published as such rather than as a zero, a
  miss, or an omission.
- **F5** — the rubric's mandated second blind scoring of one complete case is a
  positive requirement owned by `campaign-auditor-k57`; `campaign-freeze-k59`
  names the case that receives it before execution, with a rule that survives
  that case ending in shortfall; and `k64`'s unqualified "two-scorer coverage"
  exclusion is qualified the way its sibling leaves already are.
- **F6** — `transfer-freeze-k58`'s third `Done when` clause states the rubric's
  actual selection constraint rather than the self-contradicting "absent from
  the historical campaign".
- **F7** — each row of k75's clause table cites the parent `Done when` bullet it
  traces, the two deliberate merges are stated, and the authoring-conventions
  bullet is visible as covered by `deployment-verification-k71`.
- **F8** — `recovery-campaign-k54`'s `Decomposition` names
  `acceptance-replication-authority` as an external precondition settled before
  the node is entered, not as its first child.
- Every finding is classified as applied, applied differently, or declined with
  a reason. No `docs/evaluations/` record, no frozen rubric byte, and no
  committed campaign result is edited.

## Notes

F1 and F3 are requirements-shaped and their targets are `k76` and the parent
brief, not the campaign: this leaf reshapes what `k76` must decide and record,
and leaves the decision itself to that leaf and its human. Do not pre-empt it by
choosing a contract here.

F1 and F2 together may make the exact-instrument campaign look unable to produce
an acceptance-bearing result under any outcome. That conclusion is `k76`'s to
reach, not this leaf's; what this leaf owes `k76` is that the question is
unavoidable when it opens.

F2 is the one finding that touches apparatus rather than contract. Keep it
inside the boundary k75 drew: removing a tool surface the frozen access rule
already forbids is conformance, and anything that changes a prompt, row,
sampling rule, threshold, or endpoint is supplemental and belongs to
`supplemental-evaluation-k77`.

If applying a finding turns out to need substantial redesign of the reconciled
contract rather than an edit to these task files, externalise it as a new
producer leaf beside this one rather than absorbing it, per
`references/execute.md`.

## Decisions (running log)

- **F1 — applied.** `acceptance-replication-authority-k76` must confront the
  frozen report's Case A diagnosis and choose among exact replication with a
  non-removable selection limitation, failed behavioral acceptance, or an
  explicit new-instrument access amendment. k75 no longer presents option 1 as
  settled, and k72 carries the limitation when option 1 is chosen.
- **F2 — applied.** `campaign-runner-k56` owns exhaustive pre-attempt enumeration
  of reachable model-interface surfaces and proof that already-forbidden MCP,
  web, and connector surfaces are absent; `campaign-freeze-k59` records it as
  apparatus conformance. k55 and k59 make an unobtainable pinned CLI or other
  execution control a terminal unavailable exact-replication disposition.
- **F3 — applied through the requirements owner.** k76 must either amend the
  parent `Done when` or classify the historical probe as separately reported
  generality evidence and re-test its cost. k54, k68, k70, and k72 now consume
  that explicit choice and cannot promote the parent `Context` sentence by
  implication.
- **F4 — applied.** k55, the k60 brief, k64, k66, k67, and k72 state that Cases
  B and C yield descriptive counts and shortfall records only and publish both
  materiality and regression classifications as `undefined`.
- **F5 — applied.** k57 owns the historical rubric's second blind scoring as a
  positive requirement; k59 freezes its case before execution and preserves the
  choice through shortfall; k64 excludes only scorer coverage beyond that
  requirement.
- **F6 — applied.** k58 now states the actual frozen selection constraint: the
  target is outside the repository and absent from the same-case corpus and
  completed book, with the required pre-criteria blind selection chronology.
- **F7 — applied.** Every k75 trace row now identifies its parent source, the
  split of parent bullet 5 and the two deliberate merges are explicit, and
  deployment verification visibly covers the authoring-conventions clause.
- **F8 — applied.** k54 names k76 as an external sibling precondition settled
  before node entry rather than as a campaign child.
- None of the findings required substantial redesign beyond the producer and
  downstream planning contracts, so no new producer-review chain was earned.
  No historical evaluation record or frozen rubric byte was edited.
