# exposition-runs-k65

## Goal

Execute the authorized exact historical Case C prompt, no-tool rule, and
sampling contract for the contemporaneous-comparator and enabled arms.

## Context

- Joint freeze: `campaign-freeze-k59`.
- Use only the frozen runner, templates, prompt, schedule, access grammar, and
  deployed-skill manifest.

## Done when

- Five enabled and five contemporaneous-comparator repetitions follow the
  historical ABBA/BAAB schedule, invalid-run predicates, and two-replacement
  ceiling. A tool-mediated skill read keeps the historical invalid-run meaning;
  a new discovery rule cannot alter membership.
- Raw streams, manifests, prompt bytes, runtime identity, pair/order, attempt
  history, historical timing fields, and
  skill-manifest and discovery observations are complete.
- No score, skill refinement, or early cross-case conclusion occurs.

## Notes

The prompt supplies only the frozen codebase facts. Invented implementation
details remain substantive behavior to score, not a reason to replace a run.
Automatic resource-window resumption is out of scope and cannot fill or
reinterpret an acceptance shortfall.
