# deployment-verification-k71

## Goal

Determine the parent requirement's deterministic deployment conjunct against
the same skill digest the behavioral campaign executed and publish its terminal
result without selecting away a failure.

## Context

- Frozen treatment and apparatus manifest: `campaign-freeze-k59`.
- Historical harness drift guards remain historical checks, not substitutes for
  this current-digest verification.

## Done when

- The live skill's recursive manifest and aggregate digest comparison with the
  treatment frozen and checked before every campaign attempt is recorded as
  pass, failed, or unavailable.
- Current-digest positive tests exercise the reusable runner, access auditor,
  acceptance-manifest identity checks, historical replacement/sampling mode,
  refusal of supplemental fields, scorer invocation, template delta, exact
  prompts and fixtures, assignment records, bundles, and terminal outcome paths.
- Skill frontmatter, description, harness eligibility, progressive-disclosure
  bounds, citations, plugin manifest, installation/reconciliation behavior, and
  local-link check results are preserved without requiring a pass to retire the
  leaf.
- `shellcheck` and applicable repository format, lint, targeted navigation, and
  workspace-test results are preserved without rerunning selectively.
- Exact commands, runtime identity, and observed results are preserved for the
  final report. Any failed or unavailable deterministic check produces a
  terminal record that leaves the deterministic parent conjunct open; no judged
  behavior conclusion is inferred from the checks.

## Notes

A deterministic pass proves bytes and machinery, not discovery, compliance,
walkthrough quality, reader comprehension, behavioral improvement, or authority
to supersede the original failed experiment.
