# scope-runs-k61

## Goal

Execute the authorized exact historical scope-elicitation prompt and sampling
contract without selecting on skill discovery or substantive outcome.

## Context

- Joint freeze: `campaign-freeze-k59`.
- Use only the frozen runner, templates, prompt, schedule, access grammar, and
  deployed-skill manifest.

## Done when

- Five enabled and five contemporaneous-comparator repetitions follow the
  historical ABBA/BAAB schedule, Case A prompt, no-tool rule, invalid-run
  predicates, and two-replacement ceiling exactly. A visible skill-file tool
  read therefore has the historical invalid-run meaning; it cannot be legalized.
- Raw streams, complete manifests, prompt bytes, runtime identity, pair/order,
  historical timing fields, complete attempt history, and
  historical skill-manifest and discovery observations are preserved under the
  new recovery-cycle namespace.
- No behavior score, rubric edit, treatment edit, or historical rescore occurs.

## Notes

The record uses the historical sampling and shortfall semantics. Exposure-phase
resource windows are out of scope and cannot replace an attempt or denominator.
