# source-fragment-runs-k63

## Goal

Execute the authorized exact historical Case B prompt, fixture, access rule,
and sampling contract for both recovery arms.

## Context

- Joint freeze: `campaign-freeze-k59`.
- External non-Rust fixture: the historical Case B
  `targets/ocaml/check_floor.ml` bytes at their frozen digest.
- Use only the frozen fixture, runner, templates, prompt, schedule, command
  grammar, and deployed-skill manifest.

## Done when

- Five enabled and five contemporaneous-comparator repetitions follow the
  historical ABBA/BAAB schedule, declared-fixture command boundary, invalid-run
  predicates, and two-replacement ceiling. The runner does not legalize a skill-
  directory read or replace an outcome under a new exposure-phase rule.
- Fixture pre/post byte equality, raw streams, manifests, tool events, runtime
  identity, historical order and timing, complete
  attempt history, skill manifest, and discovery observations are preserved.
- No score, treatment edit, fixture refresh, or historical reinterpretation
  occurs.

## Notes

The external OCaml fixture is authoritative at its frozen digest and is never
refreshed. Resource-window behavior is out of scope and cannot fill a shortfall.
