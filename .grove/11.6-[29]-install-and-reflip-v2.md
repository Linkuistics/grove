# 11.6-[29]-install-and-reflip-v2

**Kind:** work — **USER-GATED** (confirm before the live flip)

## Goal

The live v2 flip — mirror of 070/040, one scheme version later. Build + install
the v2 binary, re-flip the live trees v1→v2 by adoption, extract the matching
global skill, verify; hand off.

## Done when

- The v2 binary (`grove` + `grove-llm`) is built green and installed (replacing
  the v1 local install).
- A reviewed `grove do` adoption-migrates this grove v1-flat → v2-dirs in one
  reviewable commit (11.4), order / done-ness / brief-chain / keys preserved.
- The global skill at `~/.claude/skills/grove/` is live and matches the v2 binary
  (content-hash stamp); idempotent re-run does nothing.
- `grove-general-improvements` re-flips by the same adoption on its next `grove do`.

## Notes

- **USER-GATED**: confirm before install + first real adoption-migrate. The
  migration commit is revertable; build + stage 11.1–11.5 first, flip as a
  deliberate reviewed step. Same choreography as 070/040 (which is the worked
  precedent — read its retired leaf).
