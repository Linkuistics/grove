# idle-activity-view-k19


## Goal
Show honest idle NEXT through the typed production observer in both full-width
views, completing idle-next-k11's product behavior.



## Context
Consume typed-observation-k18 and its landed review/integration if present.
The parent brief owns the complete acceptance matrix. Use the existing public
Viewer new/act/tick/render seam and shared select_snapshot rule.

## Done when
- Idle activity selects NEXT without exclusion and without allocating finish.
  Malformed trees refuse through the common selector. Legacy active epochs,
  Busy, Unavailable and failed tree capture withhold current row activity.
- Compare the two captures' tree and activity consistency independently;
  changing activity preserves a consistent tree. Any retained activity is
  explicitly stale. Both views, help and undersized frames keep one bounded
  retry on the existing cadence with responsive navigation and quit.
- Reserve location/view, tree observation, RUNNING, NEXT and footer lines;
  60 x 10 retains three bordered content rows. NEXT is bold normal text.
  Labels/freshness survive elision; folded/offscreen NEXT remains in summary.
- Public Viewer tests cover idle-to-legacy-active, contention and recovery,
  malformed selection, hidden activity, aliases and no-configuration read-only
  snapshots including administration state, using the real typed observer.
- Update USAGE, ARCHITECTURE, module-decomposition, CONTEXT-MAP and G6 coverage
  for landed idle activity; preserve duplicate-key repair and guard handoff
  guidance. Keep witnessed RUNNING explicitly future. Repair affected books.
- Run focused loop/tui/workspace tests and bash scripts/check.sh. Check every
  parent completion condition; leave any outstanding review/integration live.

## Notes
The witnessed-activity node follows this one. This child must not claim its
witness protocol or real RUNNING acceptance scenarios have already shipped.

## Decisions (running log)

- Consume each real `try_observe` result once per capture and compare activity
  independently of rows/content/lifetime. Withhold activity on failed tree
  acceptance; discard old activity rather than retaining a stale description.
- Keep NEXT selection in the loop's snapshot selector and carry its selected
  key into display state. Rendering only formats that accepted identity.
- Implement and verify inline: public Viewer regressions first, capture and
  chrome changes, real runtime/lock transitions, shipped-behavior documentation,
  focused crate tests, then the principal check. The scheduled k22 review owns
  adversarial review; this producer adds no competing reviewer.
- Preserve the PTY cleanup check across the new header: acknowledge help on
  its previously blank Escape line, then resize for a full frame before looking
  for its title. Differential output can reuse an existing character and is
  not a contiguous screen-text representation.
