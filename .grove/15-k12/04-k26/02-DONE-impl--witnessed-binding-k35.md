# witnessed-binding-k35


## Goal
Implement the complete typed Running mandate and verified tree relation through
the production observer, with directory-before-private precedence.



## Context
Consume released-witness-k34. The node brief and item-status spec own the full
contract. Pass the accepted TreeLifetime descriptor from capture into runtime;
do not reopen its path. Extend the validated extension into typed mandate data.

## Done when
- Same-tree requires contention on the accepted pin; previous-tree and
  no-readable-tree are explicit. Runtime identities/state/relation compare
  independently. Public try_observe positive tests identify the prepared launch.
- All marker and directory/private probe precedence cases match the spec,
  including final successful private probe overriding earlier directory errors.
- New private acquisition/probe path and descriptor replacement barriers,
  open/lock/probe errors, retry bound, missing evidence and malformed records
  are exercised. Preserve nonblocking/read-only/close-on-exec and guard release.
- Viewer compiles and withholds row activity/NEXT for Running pending k27.
  Preserve tree-failure summaries, two-capture independence, pause controls and
  read-only snapshots. Reconcile API/docs/books and run the principal gate.

## Notes
No identity-only inference and no second provider. k36 owns forced-reuse and
epoch-before-preparation mutations; k37 owns native real-process evidence.

## Decisions (running log)

Implement the reviewed algorithm in the existing runtime observer. Retain the
accepted capture's descriptor through runtime sampling; represent the verified
relation explicitly and include launch binding identities in Running equality.
Viewer presentation remains conservative until k27. The graph generation
2026-09-15T11:44:22Z does not track the loop observer files and has stale viewer
metadata, so this leaf uses direct source verification for those paths.

The private WitnessIo seam replaces only filesystem reads and shared probes.
Running carries an opaque launch-tree identity and a typed TreeRelation; its
equality also includes nonce, signal and private-witness binding. The viewer
compares that evidence independently, then withholds NEXT and row activity for
Running. Its real-control fixture covers a signal-identity change even with a
failed tree capture. Complete row binding remains k27's existing obligation.

The operation-level precedence matrix and native descriptor controls cover this
leaf's deterministic boundary. Real Started/Reaped callbacks now reach public
try_observe. The k36 forced-reuse mutations and k37 killed-holder/exec-survivor
macOS/Linux evidence remain live siblings; this host's positive launch control
does not replace either. The complete protocol review remains scheduled through
k27, so this leaf does not dispatch an additional reviewer.

## Implementation plan

1. Add a public try_observe positive control for a prepared, started launch;
   observe its failure before adding typed mandate/relation and accepted-pin
   plumbing in the two observation modules and public re-exports.
2. Implement directory-before-private probing with bounded marker reads and
   immediate unlock. Exercise marker/lock precedence, no-readable/previous-tree
   relations, descriptor replacement barriers and eight-attempt exhaustion in
   the private observer seam; retain existing pause/read-only controls.
3. Keep Viewer row activity/NEXT withheld for Running and verify independent
   two-capture comparisons and tree-failure behavior.
4. Reconcile current API/architecture and exact-source walkthrough fragments,
   run focused tests and scripts/check.sh, then retire and seal this leaf.

## Validation

The initial public Started control failed with Unavailable before implementation
and passed with typed Running afterward. Focused driver-lease and Viewer
controls passed, followed by `bash scripts/check.sh`: all eight principal checks
passed, including the complete workspace tests and all six final walkthrough
validations. The grove-loop book reconstructs 16 files with no deferred ranges.
SHA-256 digests of all 1,790 tracked input files matched before and after that
gate. Only this validation record and the retirement filename change follow it.

The public real-launch control requires Started → Running/SameTree and confirmed
Reaped → Idle. New controls cover accepted-pin path replacement, previous and
unreadable tree relations, marker prefixes/invalid/oversized bytes, directory and
private error/release precedence, acquisition and post-probe replacement with
eight-attempt exhaustion, filesystem errors and returned guard lifetimes. Viewer
tests use real locked controls through the production observer to verify
conservative presentation and independent launch-identity comparison even when
the tree fails. Existing admission, capture/runtime pauses and read-only cases
remain green. Native killed-holder/exec-survivor and mutation evidence stays with
the live sibling tasks named above.
