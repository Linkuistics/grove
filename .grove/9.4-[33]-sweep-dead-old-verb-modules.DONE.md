# 9.4-[33]-sweep-dead-old-verb-modules

**Kind:** work

## Goal

Sweep the dead old-verb modules the 060 migration left behind (root BRIEF "Dead
code to sweep"). These are neither inbox nor install — they are the *old-format*
task-tree verb path, made dead when 060 unwired it in favour of the new dotted-
decimal modules (`leaf_read`/`leaf_grow`/`leaf_lifecycle`).

## Done when

- **Deleted wholesale:** `src/pick.rs`, `src/brief_chain.rs`, `src/root_init.rs`,
  `src/leaf_ops.rs` (only comment references remain in `repo_view.rs`/`migrate.rs`)
  + their `pub mod` lines in `lib.rs` + their old-format integration tests
  (`tests/pick.rs`, `tests/brief_chain.rs`, `tests/root_init.rs`,
  `tests/leaf_ops.rs`). Confirm each is genuinely uncalled by live code first
  (`grep crate::<mod>` — the new verbs dispatch to `leaf_*`, not these).
- **`src/leaf.rs` trimmed:** delete the old-format reader/grower (`add`, `insert`,
  `surface_cross_refs`, `write_template`, the old `NNN-slug`/`done` parsing & header
  rewriting). **Keep `split_prefix`** (now the old-format reader `migrate.rs`
  consumes once) and **`Kind`** (live: the new grow/lifecycle verbs + `llm_cli`).
  Trim `tests/leaf.rs` to only the surviving `split_prefix`/`Kind` cases.
- `cargo build` + `cargo test` green with no `dead_code`/`unused` warnings for the
  swept modules.

## Notes

- `migrate.rs` itself is **kept** — it is the one-time old→new reader, still needed
  while this grove + `grove-general-improvements` flip via adoption. It is shed-able
  only once no old-format tree can exist anywhere; out of scope here.
- Independent of 9.1–9.3; ordered last only because it is the lowest-risk and
  orthogonal to the inbox/install sheds. Reorder freely.
- This is the leaf the root BRIEF said to "fold into 090 or a dedicated cleanup
  leaf" — folded in here.
