<!-- file: order=1 -->
<!-- unit: mandate-framing kinds=* class=triggering -->
What follows is your mandate: grove's own methodology, sliced for this session's
kind and delivered verbatim, then the facts grove resolved before this session
existed. It is **complete with respect to triggering conditions** — every
condition that would call for something other than carrying on with this session
is in it, so there is no situation you are expected to detect for yourself and
none you have to go looking for.

What it does not carry is procedure. Each slice arrives with its own
`<!-- unit: … -->` marker, and a marker naming `defers=<id>` has a body held
back; `grove-llm methodology <id>` serves that body verbatim, and with no
argument the verb lists every unit. The id to ask for is therefore in the text
you are already holding.
