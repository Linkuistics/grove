<!-- file: order=1 -->
<!-- unit: mandate-framing kinds=* class=triggering -->
What follows is your mandate: grove's own methodology, sliced for this session's
kind and delivered verbatim, then the facts grove resolved before this session
existed. What that guarantees is **structural**: every unit of the methodology
marked as a triggering condition for this kind is here, byte-exact, and what is
withheld is marked either procedural or for a kind that is not this one. So no
*condition* was held back for you to discover the existence of, and there is
nowhere else to look for the ones that apply to you. Recognising that one of them
*is met* remains yours: a condition describes a situation, and the situation is in
this session, not in this text.

Each slice arrives with its own `<!-- unit: … -->` marker. A marker naming
`defers=<id>` is a condition whose procedural body was held back, and
`grove-llm methodology <id>` serves that body verbatim; with no argument the verb
lists every unit. The id to ask for is therefore in the text you are already
holding. A slice naming no deferral is complete as delivered — procedure and all,
where its rule needs one.
