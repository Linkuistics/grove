<!-- unit: continue kinds=* class=triggering -->
Continue this grove — use the grove skill (Bootstrap resolves the mandated
handle below, then assembles the brief chain). If work surfaces mid-session,
externalize it as a new leaf rather than absorbing it — a new concern →
`leaf-add`/`leaf-insert`, the current item proving bigger → `leaf-decompose`
(first child only); see the skill's Decompose step. When the task is committed
(name the work item by its stable `<slug>-k<key>` handle, never its position or
path — task-tree-scheme §5) + retired, run `grove-llm complete` as your last
step to hand back to the self-driving loop. A `finish` leaf is the exception: it
*is* the complete finish cycle, so it needs explicit human confirmation before
any teardown and ends with `grove-llm complete --done` to stop the loop cleanly.
