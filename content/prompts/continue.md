Continue this grove — use the grove skill (Bootstrap assembles the brief chain first). If work surfaces mid-session, externalize it as a new leaf rather than absorbing it — a new concern → `leaf-add`/`leaf-insert`, the current item proving bigger → `leaf-decompose` (first child only); see the skill's Decompose step. When the task is committed (name the work item by its stable `<slug>-k<key>` handle, never its position or path — ADR-0035 §5) + retired, run `grove-llm complete` as your last step to hand back to the self-driving loop (or, if `pick` is empty, propose the finish cycle instead — which ends with `grove-llm complete --done` to stop the loop cleanly).
