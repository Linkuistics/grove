# External source and fragments — contemporary control repetition 1

Control repetition 1 attempt 1 exited 0 after 34 seconds, emitted a final
answer, and left the sole fixture unchanged. Its raw stream has no structured
tool event, although the answer says `wc -l`, `nl -ba`, and `/bin/cat` each
returned a sandbox-setup error. The frozen replacement predicates do not make a
normal refusal replaceable, so it remains the selected behavioral sample. The
missing direct call/result events breach the access-audit contract and prevent
using it as comparison evidence.

The blind adjudication resolves to `1/27`; only `B18` succeeds. Raw execution,
answer, prompt digest, template/home manifests, fixture pre/post manifests, and
tool-event extraction are preserved in
[`evidence/control-repetition-1-attempt-1/`](evidence/control-repetition-1-attempt-1/).
