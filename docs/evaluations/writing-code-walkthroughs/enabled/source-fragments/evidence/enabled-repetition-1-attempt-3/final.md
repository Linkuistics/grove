I can’t produce the requested authoring plan from this turn because the environment is blocking all shell reads, including a trivial `pwd` check, with:

```text
sandbox-exec: sandbox_apply: Operation not permitted
```

That means I could not inspect the sole authoritative source file, `targets/ocaml/check_floor.ml`. Since your request requires exact, source-grounded outputs, I won’t fabricate the source inventory, fragment ledger, reader sequence, worked execution, or publication checks.

If you want, send the contents of `targets/ocaml/check_floor.ml` in the chat, or rerun with functioning read-only shell access, and I’ll return the complete plan in one pass.
