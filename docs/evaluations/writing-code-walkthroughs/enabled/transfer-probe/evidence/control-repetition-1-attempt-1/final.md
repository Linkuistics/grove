The authoritative source inventory for this run is:

- `bin/fzf-tmux`

I can’t produce the requested source-grounded authoring plan from this run because local read-only inspection is currently blocked at the tool layer. Every attempt to read files in `/private/tmp/grove-enabled-transfer.djF4XX/control-repetition-1-attempt-1/run-directory` failed with `sandbox-exec: sandbox_apply: Operation not permitted`, and there is no alternate local file-reading connector exposed in this session. I also did not fetch or rely on any outside source material.

If you want the actual plan, either:
1. rerun with working read access to the run directory, or
2. paste the contents of `bin/fzf-tmux` here.

Once the file is readable, I can produce the exact walkthrough authoring plan you asked for: reader sequence, fragment ledger with insertion relationships, coarse execution trace, exposition and cross-reference decisions, and completion/review assurance checks.
