I can only state the exact authoritative inventory from your prompt: `bin/fzf-tmux`.

I cannot produce the rest of the requested authoring plan faithfully from this run because the environment prevented any local file inspection. Every read attempt through `exec_command` failed before process start with `sandbox-exec: sandbox_apply: Operation not permitted`, and there is no alternate MCP resource exposing the workspace contents. Given your constraint to use only read-only inspection inside this fresh run directory and not assume any other source material, anything beyond the inventory would be guesswork.

If you want, I can continue as soon as the file becomes readable in this environment, or you can paste the contents of `bin/fzf-tmux` and I’ll turn around the full plan from that text only.
