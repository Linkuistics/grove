I can only state the authoritative inventory with confidence in this run:

- `bin/fzf-tmux`

I cannot responsibly produce the rest of the requested authoring plan from source, because this session does not expose any usable read path into the workspace file. The terminal executor is failing at the sandbox layer (`sandbox-exec: sandbox_apply: Operation not permitted`), and no alternate local file-reading tool is available in the run.

That means I would have to invent the concept sequence, fragment ledger, representative execution, and section-level exposition choices without inspecting `bin/fzf-tmux`, which would violate your constraints. If you want the actual plan, rerun with working read access to the run directory or paste `bin/fzf-tmux`; then I can produce the complete source-grounded plan in one pass.
