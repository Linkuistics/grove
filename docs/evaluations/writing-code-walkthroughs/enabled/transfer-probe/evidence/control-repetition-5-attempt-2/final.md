I can only establish item `1` with confidence from this run:

- Full authoritative source inventory: `bin/fzf-tmux`

I cannot produce the rest of the requested authoring plan without inventing details from unseen code, which would violate your constraint to use only read-only inspection inside this fresh run directory. In this environment, every attempt to read the workspace file failed with the same local sandbox error: `sandbox-exec: sandbox_apply: Operation not permitted`, and no alternate MCP file-content reader is exposed for the run directory.

Because of that, I’m not going to fabricate:

- a concept-oriented reader sequence grounded in the script’s actual structure
- a fragment ledger with exact quoted/paraphrased source fragments and insertion ordering
- a representative execution trace
- section-by-section exposition and cross-reference decisions
- complete-inventory and independent-review assurance checks tied to the real contents

If you rerun with working local read access, or provide the contents of `bin/fzf-tmux`, I can produce the full authoring plan in one pass.
