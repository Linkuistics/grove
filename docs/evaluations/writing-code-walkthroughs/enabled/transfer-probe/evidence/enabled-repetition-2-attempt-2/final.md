I can only complete item 1 from this run.

The exact authoritative source inventory is:

- `bin/fzf-tmux`

I can’t responsibly produce items 2 through 6 because this environment did not permit any local file inspection. The available shell reads all failed at sandbox startup, and the MCP resources exposed in this session do not include the workspace files, so I was unable to read `bin/fzf-tmux` at all. Given your constraint to use read-only inspection only inside this fresh run directory and not assume any other source material, inventing a reader sequence, fragment ledger, execution trace, or coverage assurances from unseen code would be incorrect.

If you re-run with functioning local read access, or paste the contents of `bin/fzf-tmux`, I can produce the full authoring plan in one pass.
