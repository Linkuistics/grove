I can’t produce the requested authoring plan responsibly in this session because local read access is broken: every `exec_command` call, including `pwd` and direct reads of `bin/fzf-tmux`, fails with `sandbox-exec: sandbox_apply: Operation not permitted`, and I did not find any alternate workspace file-reading resource.

Given your constraints, I should not infer `bin/fzf-tmux` from memory or external sources. If you rerun this with working read-only shell access, or paste the contents of `bin/fzf-tmux` here, I can then produce the exact plan you asked for:
1. full authoritative inventory,
2. concept-oriented reader sequence,
3. fragment ledger with insertion relationships,
4. coarse worked execution,
5. section-by-section exposition and cross-reference decisions,
6. coverage and independent-review assurance checks.
