I can only state item `1` exactly from the prompt:

- Authoritative source inventory: `bin/fzf-tmux`

I can’t produce the rest of the requested authoring plan responsibly in this run, because the environment is not actually inspectable from my side. Every read attempt through the provided shell tool failed before execution with `sandbox-exec: sandbox_apply: Operation not permitted`, and no alternate local file-reading/resource tool is exposed for the fresh run directory. Under your constraint not to fetch, browse, or assume outside material, I should not invent the concept sequence, fragment ledger, or execution trace for `bin/fzf-tmux` without reading it.

If you want the exact plan, either:
1. paste the contents of `bin/fzf-tmux` here, or
2. rerun with working read access to the fresh run directory.

With either of those, I can produce the complete-source authoring plan you asked for in one pass.
