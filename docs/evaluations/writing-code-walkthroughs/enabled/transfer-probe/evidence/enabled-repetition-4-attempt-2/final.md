I can’t produce a truthful complete-source authoring plan from this run as-is, because I was unable to inspect the only authoritative source file.

The exact authoritative inventory is:

- `bin/fzf-tmux`

The blocker is local read access in this environment. Every shell read attempt failed with:

```text
sandbox-exec: sandbox_apply: Operation not permitted
```

I also checked for alternate local-content readers and connected document surfaces in this run; none were available for `bin/fzf-tmux`.

Because your instructions prohibit assuming any other source material, I’m not going to invent:
- the concept sequence,
- the fragment ledger,
- the worked execution,
- the section-level exposition/cross-reference decisions, or
- the coverage assurance pass

without reading that file first.

If you want, rerun this with working read access to the run directory, or paste `bin/fzf-tmux` here, and I’ll produce the full six-part authoring plan from that source only.
