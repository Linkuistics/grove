---
name: grove-integrate-review-planning
description: The `integrate-review-planning` session kind — triage one `review-planning` leaf's findings and apply the real ones. Use when a grove mandate names this skill, or when running an `integrate-review-planning` session in a grove working tree.
harnesses: [any]
---

# integrate-review-planning

**Load the `grove` skill now** — on Claude Code, where plugin skills are
namespaced, that is `grove:grove` — and read `references/integrate-review.md`
in it.

The five `integrate-review-*` kinds are one family with one procedure, and that
file is it: the goal, the shared triage, where the findings are read from, what
each of the five may change, and what is too large for this session. This skill
states none of it a second time. Everything else — the constraints, the
bootstrap, and the execute, decompose, retire and commit procedures — is the
spine's.

The review you are integrating is the `review-planning` leaf named by handle
in your task body.
