---
name: grove-research-b
description: The `research-b` session kind — a citation-disciplined, breadth-seeking literature and prior-art survey. Use when a grove mandate names this skill, or when running a `research-b` session in a grove working tree.
harnesses: [any]
---

# research-b

**Load the `grove` skill now** — on Claude Code, where plugin skills are
namespaced, that is `grove:grove` — and read `references/research.md` in it.

`research-a` and `research-b` are one family with one procedure, and that file
is it: the goal, what the survey owes, what the pair owes each other, and which
output path each half writes. This skill states none of it a second time.
Everything else — the constraints, the bootstrap, and the execute, decompose,
retire and commit procedures — is the spine's.

You are the **b** half; the pair's other leaf is `research-a`.
